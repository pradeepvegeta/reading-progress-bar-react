/**
 * Component Exports
 *
 * Central export point for all React components used in the plugin.
 * Import from this index rather than from individual component files so
 * that internal file moves do not require changes at call sites.
 *
 * Exported components:
 *  - SettingsPage : full admin settings page (mount point: #reading-progress-bar-settings)
 *
 * @since 1.0.0
 */

export { SettingsPage } from './settings-page';